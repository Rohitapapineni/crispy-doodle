create table SPEED(Username varchar(50), Date_Time varchar(50), Speed float, FOREIGN KEY Username);
create tabel USERS(Username varchar(50), Email_ID varchar(50), Password varchar(50), PRIMARY KEY Username);